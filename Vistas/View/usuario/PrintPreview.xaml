<Window x:Class="Vistas.View.usuario.PrintPreview"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:scm="clr-namespace:System.ComponentModel;assembly=WindowsBase"        
         xmlns:data="clr-namespace:ClaseBase;assembly=ClaseBase"
        Title="PrintPreview" Height="537" Width="375">
    <Window.Resources>
        <ObjectDataProvider
            x:Key='LIST_USERS'
            ObjectType="{x:Type data:TrabajarUsuario}"
            MethodName="obtenerUsuarios">
        </ObjectDataProvider>
        <CollectionViewSource x:Key='VIEW_LIST_USERS' Source="{StaticResource ResourceKey='LIST_USERS'}">
            <CollectionViewSource.SortDescriptions>
                <scm:SortDescription PropertyName="Usu_UserName" Direction="Ascending"></scm:SortDescription>
            </CollectionViewSource.SortDescriptions>
        </CollectionViewSource>
    </Window.Resources>  
    
   
    <StackPanel Width="349">
        <FlowDocumentReader  Name="DocReader" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" >
            <FlowDocument Name="DocMain">
                <Paragraph>Listado de Usuarios </Paragraph>
                <BlockUIContainer>
                    <ListView ItemsSource="{Binding Usuarios}">
                        <ListView.View>
                            <GridView>
                                <GridView.Columns>
                                    <GridViewColumn Header="Username" DisplayMemberBinding="{Binding Path=Nickname}" Width="150"/>
                                    <GridViewColumn Header="Nombre Completo" DisplayMemberBinding="{Binding Path=NombreCompleto}" Width="200"/>
                                    <GridViewColumn Header="Rol" DisplayMemberBinding="{Binding Path=Rol.Descripcion}" Width="100"/>
                                </GridView.Columns>
                            </GridView>

                        </ListView.View>
                    </ListView>
                </BlockUIContainer>
                
            </FlowDocument>
            
        </FlowDocumentReader>
        <Button Content="Imprimir" Width="100" Height="28" HorizontalAlignment="Center" Margin="0, -50, 10, 70" Click="Imprimir_Click"/>
    </StackPanel>
</Window>
