<UserControl x:Class="Vistas.View.usuario.UserView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             Background="Transparent"
             Loaded="UserView_Loaded"
             d:DesignHeight="450" d:DesignWidth="800">
    <Border CornerRadius="10"
            BorderThickness="3"
            Background="Transparent">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="250"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Menu de Navegación -->
            <Border CornerRadius="10,0,0,10">
                <Border.Background>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,0.7">
                        <GradientStop Color="#1de2a0" Offset="0"/>
                        <GradientStop Color="{StaticResource tercerBackColor1}" Offset="1"/>
                    </LinearGradientBrush>
                </Border.Background>

                <StackPanel>
                    <!-- Logo -->
                    <StackPanel Orientation="Horizontal" Height="35" Margin="15,25,0,40">
                        <!--<Image Source="/images/logofacu.png" Height="30"/>
                        <TextBlock Text="Grupo 3 - Apu"
                                   Foreground="{StaticResource titleColor2}"
                                   FontSize="20"
                                   FontFamily="Consolas"
                                   FontWeight="Medium"
                                   VerticalAlignment="Center"
                                   Margin="10,0,0,0"/>-->
                    </StackPanel>
                    <RadioButton Style="{StaticResource menuButton}" Tag="{StaticResource color1}" Cursor="Hand" x:Name="btnGestion" Click="btnGestion_Click">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Style="{StaticResource menuButtonIcon}">&#xf0c0;</TextBlock>
                            <TextBlock Text="Gestión" Style="{StaticResource menuButtonText}"/>
                        </StackPanel>
                    </RadioButton>
                    <RadioButton Style="{StaticResource menuButton}" Tag="{StaticResource color2}" Cursor="Hand" x:Name="btnOrdenamiento" Click="btnOrdenamiento_Click">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Style="{StaticResource menuButtonIcon}">&#xf0e7;</TextBlock>
                            <TextBlock Text="Ordenamiento" Style="{StaticResource menuButtonText}"/>
                        </StackPanel>
                    </RadioButton>
                    <RadioButton Style="{StaticResource menuButton}" Tag="{StaticResource color3}" Cursor="Hand" x:Name="btnLista" Click="btnLista_Click">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Style="{StaticResource menuButtonIcon}">&#xf03a;</TextBlock>
                            <TextBlock Text="Lista" Style="{StaticResource menuButtonText}"/>
                        </StackPanel>
                    </RadioButton>
                </StackPanel>
            </Border>

            <!-- Área de Contenido -->
            <Border CornerRadius="0,10,10,0" Grid.Column="1">
                <Border.Background>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,0.7">
                        <GradientStop Color="#cdfee6" Offset="0"/>
                        <GradientStop Color="{StaticResource tercerBackColor1}" Offset="1"/>
                    </LinearGradientBrush>
                </Border.Background>
                <ContentControl x:Name="subContenArea" Margin="25"/>
            </Border>
        </Grid>
    </Border>
</UserControl>
